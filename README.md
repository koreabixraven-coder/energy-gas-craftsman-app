# energy-gas-craftsman-app v3.17

## v3.17 변경
- 에너지관리기능사 2005년 10월 02일 교사용 PDF 60문제 추가
- 본문 검은 정답표시와 최종 정답표 60문제 전수 대조: 불일치 0
- PDF 정답 ①②③④를 앱 ans 0·1·2·3으로 변환
- 해설 원문이 없는 문제는 ex: ""로 처리
- 기존 v3.16의 7,112문제와 가스 심화종합반·기존 기능·이미지 보존
- 전체 7,172문제 / DB 185개 묶음
- 전체 정답 인덱스 0~3, ans:4 없음
- TTS, 자동읽기, 오답노트, 암기완료, 모바일 하단 다크 보정 유지

배포 확인 시 앱 주소에 `?v=3.17`을 붙입니다.

## 업데이트 ZIP 안내
이 ZIP은 v3.16 업데이트본을 기준으로 만든 업데이트본입니다. 기존 운영 폴더에 덮어쓸 때 기존 `assets`의 다른 이미지와 `icon-*.png` 파일은 삭제하지 마십시오.


## v3.17 강제 캐시 업데이트 보정
- 서비스워커 등록 URL을 `sw.js?v=3.17`로 변경
- `updateViaCache: "none"` 적용으로 서비스워커 스크립트의 HTTP 캐시 우회
- `theory.js?v=3.17`로 핵심 파일 버전 쿼리 통일
- 서비스워커 설치 시 기존 `energy-gas` 캐시 삭제, `skipWaiting()` 및 `clients.claim()` 유지
